# Build output — regenerated from ../index.html by `npm run build`
www/

# Dependencies
node_modules/

# Capacitor native project — generated by `npx cap add ios`.
# The Xcode project itself SHOULD be committed once it exists (signing config,
# widget target, entitlements). Only its build artifacts are ignored.
ios/App/Pods/
ios/App/build/
ios/App/App/public/
ios/DerivedData/

# macOS
.DS_Store
